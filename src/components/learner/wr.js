import React from "react";
import Spreader from "./spreader";

/*
class WR
    extends React.Component {
    render({le}) {
        return(
            <h1>{le}</h1>
        )
    }
}

export default WR;

 */
